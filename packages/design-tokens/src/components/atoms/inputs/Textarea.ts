/**
 * Textarea Component - Multi-line text input field
 * @stub - Implementation pending
 */

import type { TextareaProps } from '../../../types/components.js';

/**
 * Textarea component for multi-line text entry
 *
 * @example
 * ```tsx
 * <Textarea
 *   placeholder="Enter your message"
 *   label="Message"
 *   autoResize
 *   maxLength={500}
 *   showCharCount
 * />
 * ```
 */
export const Textarea = (_props: TextareaProps) => {
  // Implementation pending
  throw new Error('Textarea component not yet implemented');
};

Textarea.displayName = 'Textarea';
